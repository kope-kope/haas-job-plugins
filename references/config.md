# Plugin Configuration

This file is created during `/setup` and stores IDs needed by the skills.

## Google Drive

- Job Search Folder ID: (not configured — run /setup Step 1b)
- Job Search Folder URL: (not configured)
- Master Resume Doc ID: (not configured — run /setup Step 1b)
- Master Resume URL: (not configured)
